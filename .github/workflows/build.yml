name: Build and Push Docker Image
# Build the agent-backend Docker image and push to GCR.

on:
  workflow_call:
    inputs:
      image_tag:
        description: 'Image tag (e.g. v2-latest, release-candidate)'
        required: false
        type: string
        default: 'v2-latest'
  workflow_dispatch:
    inputs:
      image_tag:
        description: 'Image tag'
        required: true
        default: 'v2-latest'
        type: choice
        options:
        - v2-latest
        - release-candidate

env:
  IMAGE_BASE: gcr.io/yupp-llms/agent-backend
  IMAGE_TAG: ${{ github.event.inputs.image_tag || inputs.image_tag || 'v2-latest' }}
  GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}

permissions:
  contents: read
  packages: read

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Authenticate to Google Cloud
      uses: ./.github/actions/gcp-auth
      with:
        gcp_project_id: ${{ env.GCP_PROJECT_ID }}
        credentials_json: ${{ secrets.GCP_SA_KEY }}

    - name: Configure Docker for GCR
      run: gcloud auth configure-docker gcr.io --quiet

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Generate build metadata
      run: |
        echo "BUILD_GIT_SHA=${{ github.sha }}" >> .env
        echo "BUILD_TIMESTAMP=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> .env
        echo "BUILD_GIT_BRANCH=${{ github.ref_name }}" >> .env

    - name: Build and push Docker image
      uses: docker/build-push-action@v6
      with:
        context: .
        platforms: linux/amd64
        push: true
        tags: |
          ${{ env.IMAGE_BASE }}:${{ env.IMAGE_TAG }}
          ${{ env.IMAGE_BASE }}:sha-${{ github.sha }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Print image info
      run: |
        {
          echo "## Docker Image Built"
          echo "| Setting | Value |"
          echo "|---------|-------|"
          echo "| Image | \`${{ env.IMAGE_BASE }}:${{ env.IMAGE_TAG }}\` |"
          echo "| SHA tag | \`${{ env.IMAGE_BASE }}:sha-${{ github.sha }}\` |"
          echo "| Git SHA | \`${{ github.sha }}\` |"
        } >> $GITHUB_STEP_SUMMARY
