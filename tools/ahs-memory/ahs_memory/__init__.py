"""``ahs-memory`` — bulk-import a local Markdown workspace into AHS as MEMORY artifacts.

Public surface is the ``cli`` entry point (registered as ``ahs-memory`` and
``python -m ahs_memory``); the rest of the package is implementation detail.
"""

from __future__ import annotations

__version__ = "0.1.0"
